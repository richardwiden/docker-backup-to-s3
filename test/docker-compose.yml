version: '3'
networks:
  local:
    internal: true
services:
  backup:
    build:
      dockerfile: Dockerfile
      context: ../
    command: echo hello world2
    networks:
      - local
  minio:
    image: altmannmarcelo/minio:latest
    networks:
      - local